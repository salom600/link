# ============================================================================
# LinkOS — systemd-boot loader config (UEFI)
# ============================================================================

# Default boot entry
default linkos

# Show menu for 5 seconds, then boot default
timeout 5

# Use console-mode 0 (highest resolution)
console-mode max

# Editor disabled for security
editor no

# Auto-firmware reboot
auto-entries yes
auto-firmware yes
