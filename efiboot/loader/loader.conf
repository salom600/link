# ============================================================================
# LinkOS — systemd-boot loader config (UEFI)
# Modern archiso uses archisosearchuuid (more reliable than label-based search)
# ============================================================================

timeout 15
default 01-linkos.conf
beep on
