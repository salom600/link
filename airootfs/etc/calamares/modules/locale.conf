# ============================================================================
# LinkOS — Calamares locale module
# ============================================================================

# Locale to set on the target system
locale: en_US.UTF-8

# Available locales (user can pick from these)
localeGenPath: /etc/locale.gen

# Timezone — auto-detected from the live session, fallback to UTC
region: America
zone: New_York
