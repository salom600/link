# ============================================================================
# LinkOS — Calamares machineid module
# Generates fresh /etc/machine-id and entropy on the installed system.
# ============================================================================

# Generate a new /etc/machine-id on first boot
systemd: true

# Generate entropy file
dbus: true

# Symlink /etc/machine-id to /var/lib/dbus/machine-id
symlink: true
