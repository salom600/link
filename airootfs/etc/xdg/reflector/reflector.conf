# LinkOS — reflector config (auto-updates pacman mirrorlist)
# Saves the 30 most responsive HTTPS mirrors, sorted by download speed.
--save /etc/pacman.d/mirrorlist
--protocol https
--country us,gb,de,fr,nl,se
--latest 30
--sort rate
--download-timeout 30
